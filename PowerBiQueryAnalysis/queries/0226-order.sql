-- Power BI query shape 226 of 663, from workspace_jf.landscape.qh.
-- Not a schema probe: this shape reads data and returns rows.
--
-- Executions            220
-- Distinct texts        3 (same query, different literals or projection)
-- Rows read             5,814,240
-- Rows returned         5,814,240
-- Avg duration          1,056 ms
-- Power BI datasets     none recorded
-- Tables                prod_homecare_actransactional_ordermanagement.order
--
-- Power BI's `SELECT ... FROM (...) AS `_`` envelope was stripped, so this
-- is the inner query as written, reformatted by sqlglot -- not the exact
-- bytes Power BI sent.

SELECT
  *
FROM prod_homecare_actransactional_ordermanagement.order
