-- Power BI query shape 38 of 663, from workspace_jf.landscape.qh.
-- Not a schema probe: this shape reads data and returns rows.
--
-- Executions            2,196
-- Distinct texts        1 (same query, different literals or projection)
-- Rows read             2,184
-- Rows returned         13,176
-- Avg duration          337 ms
-- Power BI datasets     3fdd5aed-f1c1-47e1-b7b0-5d4b81493a8d, aa36e346-e2cc-4210-a366-8c48278fcdbd
-- Tables                prod_homecare_insite_directory.slppsourcetype
--
-- Power BI's `SELECT ... FROM (...) AS `_`` envelope was stripped, so this
-- is the inner query as written, reformatted by sqlglot -- not the exact
-- bytes Power BI sent.

SELECT
  *
FROM prod_homecare_insite_directory.slppsourcetype
